logger:
  level: "info"

kafka:
  brokers:
    - "localhost:9092"

order_paid_consumer:
  topic: "order.paid"
  group_id: "assembly-service"

ship_assembled_producer:
  topic: "ship.assembled"

# Эмуляция сборки корабля (в секундах)
assembler:
  min_build_time_sec: 5
  max_build_time_sec: 15
