package orderpaid_test

// TODO(неделя 5): unit-тесты обработчика OrderPaid —
//  - валидное сообщение декодируется и вызывает AssemblyService.Assemble;
//  - битое сообщение → ошибка (не коммитим);
//  - идемпотентность: повторный event_uuid не запускает сборку дважды.
