syntax = "proto3";

package inventory.v1;

import "google/protobuf/timestamp.proto";

option go_package = "github.com/1mpuser/shared/pkg/proto/inventory/v1;inventoryv1";

// InventoryService управляет каталогом деталей для космических кораблей
service InventoryService {
  // GetPart возвращает деталь по UUID
  rpc GetPart(GetPartRequest) returns (GetPartResponse);

  // Возвращает список деталей с возможностью фильтрации по типу или по конкретным UUID.
  rpc ListParts(ListPartsRequest) returns (ListPartsResponse);
}

// Тип детали
enum PartType {
  // Не указано
  PART_TYPE_UNSPECIFIED = 0;

  // Корпус корабля
  PART_TYPE_HULL = 1;

  // Двигатель
  PART_TYPE_ENGINE = 2;

  // Щит
  PART_TYPE_SHIELD = 3;

  // Оружие
  PART_TYPE_WEAPON = 4;
}

// Part представляет деталь космического корабля
message Part {
  // Уникальный идентификатор детали
  string uuid = 1;

  // Название детали
  string name = 2;

  // Описание детали
  string description = 3;

  // Цена в копейках (5000₽ = 500000)
  int64 price = 4;

  // Тип части
  PartType part_type = 5;

  // Количество на складе
  int64 stock_quantity = 6;

  // Дата создания
  google.protobuf.Timestamp created_at = 7;
}

// Запрос на получение детали по UUID
message GetPartRequest {
  // ID шник детали
  string uuid = 1;
}

// Возвращаем информацию о детали
message GetPartResponse {
  // Возвращаем информацию о детали
  Part part = 1;
}

// Возвращаем информацию о деталях
message ListPartsRequest {
  // Фильтр по типу (UNSPECIFIED = все детали)
  PartType part_type = 1;

  // Фильтрация по uuid
  repeated string uuids = 2;
}

// Возвращаем информацию о деталях
message ListPartsResponse {
  // Отфильтрованные части
  repeated Part parts = 1;
}
